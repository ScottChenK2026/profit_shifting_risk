"""The model zoo for the project, gathered in one package.

Four approaches that I compare against each other:
  - the MLP, a straightforward neural net classifier (the simple deep baseline);
  - the FT-Transformer, a modern attention-based net (the main showcase);
  - the autoencoder, an unsupervised anomaly detector that ignores the labels;
  - and XGBoost, the strong conventional tree-based baseline to beat.
"""
