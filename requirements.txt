# Profit-shifting risk detection - pinned dependencies
# Install with:  pip install -r requirements.txt   (Python 3.11)
#
# These are the exact versions the reported results were produced with. Pinning them means a fresh
# install gets the same libraries on Windows, macOS and Linux, and that the saved scaler and models
# load without version warnings.
#
# Note on PyTorch: the default PyPI wheel works on Windows, Linux and Apple-silicon Macs. PyTorch no
# longer publishes wheels for Intel Macs, so those machines cannot install torch 2.14. For a
# guaranteed CPU-only build on Linux, install from the official index:
#   pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
#
# macOS also needs the OpenMP runtime for XGBoost - see "Platform notes" in the README.

numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
scipy==1.17.1        # DeLong test, rank transforms; also a scikit-learn dependency
xgboost==3.2.0
torch==2.14.0
shap==0.51.0
matplotlib==3.10.9
seaborn==0.13.2
pytest==9.1.1
